using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttachPoint : MonoBehaviour
{
    /*
    [SerializeField] private Attachable part = null;
    private void OnTriggerEnter(Collider other)
    {
        if (!part.Attached)
        {
            MainPart mainPart = other.GetComponent<MainPart>();
            if (mainPart)
                mainPart.AddToCloseParts(part);
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (!part.Attached)
        {
            MainPart mainPart = other.GetComponent<MainPart>();
            if (mainPart)
                mainPart.RemoveFromCloseParts(part);
        }
    }
    */

}
